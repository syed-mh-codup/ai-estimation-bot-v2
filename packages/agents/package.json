{
  "name": "@repo/agents",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run",
    "embed:presets": "tsx src/scripts/embed-presets.ts"
  },
  "dependencies": {
    "@mastra/core": "^1.41.0",
    "@repo/db": "workspace:*",
    "@repo/providers": "workspace:*",
    "@repo/shared": "workspace:*",
    "mammoth": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "tsx": "^4.16.2"
  }
}
