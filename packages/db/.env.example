# Copy to packages/db/.env — used by the Prisma CLI (migrate/generate) and the
# bootstrap seed (`pnpm --filter @repo/db db:seed`).
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/ai_estimation?schema=public"
